/**
 * Example script: create-seaport-order.example.js
 *
 * This is a template showing how to create a sell order using Seaport / OpenSea
 * SDK. It is provided as an example only; to run it you must install the
 * dependencies and provide a PRIVATE_KEY and RPC_URL in your .env. Executing
 * order creation moves real assets and requires your wallet's private key.
 *
 * WARNING: Do not paste your private key into chat. Run locally only.
 */

// Example placeholder — requires @opensea/seaport or compatible library
console.log('See comments in this file. Implement local order creation with care.');

/*
const { Seaport } = require('@opensea/seaport-js');
const { ethers } = require('ethers');
require('dotenv').config();

async function main() {
  const provider = new ethers.providers.JsonRpcProvider(process.env.RPC_URL);
  const wallet = new ethers.Wallet(process.env.PRIVATE_KEY, provider);
  const seaport = new Seaport(wallet);

  // Example: create a sell order for tokenId on contract
  // Refer to seaport docs for exact order construction
}

main();
*/
